After cloning project, run npm install in project root directory.

npm start


The project can be tested on the browser by pressing the 'w' key in the terminal window, or on an iOS device after downloading expo pro from the app store and scanning the QR code provided by the terminal.
